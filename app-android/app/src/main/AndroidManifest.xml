<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lembretes de remédio precisam tocar na hora certa, mesmo com o celular em repouso. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- O alarme do remédio: serviço em primeiro plano que toca som (não é encerrado pelo Android), vibra, mantém o
         processador acordado e abre a tela por cima da tela de bloqueio. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Dados de saúde: sem backup na nuvem, e só tráfego HTTPS (sem texto puro). -->
    <application
        android:name=".CuidaMedApp"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CuidaMed">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CuidaMed"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Push do Firebase: só avisa "há novidade"; o app busca os dados e monta a notificação. -->
        <service android:name=".notificacoes.ServicoDePush" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Entrega o PDF do histórico (guardado no cache do app) ao leitor de PDF ou ao app de compartilhar escolhido. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.arquivos"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/arquivos_compartilhados" />
        </provider>

        <service
            android:name=".notificacoes.ServicoDoAlarme"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- A tela do alarme: aparece por cima da tela de bloqueio e liga a tela. -->
        <activity
            android:name=".notificacoes.AlarmeActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.CuidaMed" />

        <receiver android:name=".notificacoes.LembreteReceiver" android:exported="false" />
        <receiver android:name=".notificacoes.PararAlarmeReceiver" android:exported="false" />
        <receiver android:name=".notificacoes.TomeiReceiver" android:exported="false" />
        <!-- Depois de reiniciar o celular ou atualizar o app, os alarmes precisam ser marcados de novo. -->
        <receiver android:name=".notificacoes.ReinicioReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>